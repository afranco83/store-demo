API_URL="http://localhost:4000"
# Secret propio de Auth.js (cifra su cookie de sesión) — independiente de
# AUTH_JWT_SECRET de apps/api. Genera uno con `npx auth secret` o
# `openssl rand -base64 32`.
AUTH_SECRET="changeme"
