name: Release

# Corre solo tras un éxito de CI en main, nunca sobre código sin verificar.
on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

permissions:
  contents: write
  # Exigidos por @semantic-release/github, aunque el comentario automático
  # en issues/PRs está desactivado (successComment: false en .releaserc.json)
  # para no generar ruido en un repo de un único mantenedor — solo se usa
  # para crear la Release.
  issues: write
  pull-requests: write

jobs:
  release:
    if: github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    env:
      # pnpm install en la raíz dispara el postinstall de todos los
      # workspaces, incluido el de apps/api (prisma generate), que exige
      # DATABASE_URL para poder cargar prisma.config.ts aunque este
      # workflow no toque apps/api en ningún momento — mismo hallazgo que
      # ya documentó la Fase 8 para el despliegue en Vercel (ver adenda de
      # docs/ROADMAP.md). Placeholder, nunca se conecta de verdad.
      DATABASE_URL: "file:./dev.db"
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          # semantic-release necesita ver todo el historial de tags para
          # calcular el rango de commits desde el último release.
          fetch-depth: 0

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: pnpm exec semantic-release
